openapi: 3.1.0
info:
  title: Example Provider API
  version: 0.1.0
servers:
  - url: https://example-staging.api.your-vendor.com
paths:
  /health:
    get:
      operationId: healthCheck
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                required:
                  - status
